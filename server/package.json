{
  "name": "server",
  "version": "1.0.0",
  "description": "This is a mock products server build with json server",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "json-server --watch ./database/db.json"
  },
  "author": "Rasel",
  "license": "MIT",
  "dependencies": {
    "json-server": "^1.0.0-alpha.23"
  }
}
